<Window x:Class="EquipmentAccountingIS.App.Windows.StoredUnits.DeleteStoredUnitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Удаление хранимой еденицы" Background="Gray" Height="180" Width="400">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="7*" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="1" Grid.Column="1">
            <GroupBox Header="Примечание к удалению">
                <RichTextBox Name="rtbNote" />
            </GroupBox>
        </Grid>
        <Button Grid.Row="2" Grid.Column="1" Margin="100,10,100,0" Content="Удалить" Click="ButtonBase_OnClick" />
    </Grid>
</Window>